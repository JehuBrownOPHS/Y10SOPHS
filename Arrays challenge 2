cars = []
sentry = True
while sentry == True:
    brand = str(input("input a car brand or press x to exit >>> "))
    if brand == "x":
        print("the list is" , cars)
        print("exiting")
        sentry = False
    else:
        cars.append(brand)
